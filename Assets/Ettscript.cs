using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ettscript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //change some behaviour
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
